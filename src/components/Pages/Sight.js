import BackGround from "../Molecules/BackGround";

export default function Sight() {
    return (
        <div className="Sight">
            Je suis le Sight
            <BackGround />
        </div>
    )

    // Bouton PLAY pour lancer l'affichage des cercles + lance l'enregistrement du score
    // Un input pour afficher le score
    // Afficher un timer
    // Qd je clique, le bouton disparait et apparait ailleurs
}